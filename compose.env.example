# Example values for docker compose environment expansion
# Bind UI to all interfaces for LAN access (override as needed)
UI_BIND_IP=0.0.0.0
# Leave empty to let the UI generate a random first-run password (written to ui/data/admin_password.generated)
UI_ADMIN_PASSWORD=
# Provide a persistent session secret so logins survive container recreation (auto-generated if empty)
UI_SESSION_SECRET=
# UID/GID used when writing secrets.properties so kafka-connect can read it
TS_CONNECT_SECRETS_UID=1000
TS_CONNECT_SECRETS_GID=1000
# Optional: shared secret between UI and update agent (auto-generated if empty)
TS_CONNECT_UPDATE_AGENT_TOKEN=
# URL where the UI can reach the update agent; default resolves to the container on the shared network
TS_CONNECT_UPDATE_AGENT_URL=http://ts-connect-update-agent:9000
# Optional: override workspace path if docker compose runs from a different directory
# TS_CONNECT_WORKSPACE_HOST=/home/youruser/connector
# Optional: override host gateway mapping for host.docker.internal
# TS_CONNECT_HOST_GATEWAY_OVERRIDE=host.docker.internal:host-gateway

# Container Image
TS_CONNECT_UI_IMAGE=targetshot.azurecr.io/ts-connect:stable
# Optional: use another tag for the update check (falls back auf TS_CONNECT_UI_IMAGE)
# TS_CONNECT_UPDATE_CHECK_IMAGE=targetshot.azurecr.io/ts-connect:stable
# Optional: build local images during updates instead of pulling from ACR
# TS_CONNECT_UPDATE_BUILD_LOCAL=false

# Host-Agent für Betriebssystem-Updates (optional, läuft direkt auf dem Host)
# TS_CONNECT_HOST_AGENT_URL=http://127.0.0.1:9010
# TS_CONNECT_HOST_AGENT_TOKEN=
# TS_CONNECT_HOST_REBOOT_DELAY=60

# Azure Container Registry Auth (optional)
# TS_CONNECT_ACR_REGISTRY=targetshot.azurecr.io
# TS_CONNECT_ACR_USERNAME=
# TS_CONNECT_ACR_PASSWORD=

# Offline-Puffer / Backup-Datenbank
TS_CONNECT_BACKUP_DB=targetshot_backup
TS_CONNECT_BACKUP_USER=targetshot
# Passwort wird beim Start automatisch zufällig gesetzt (optional initialer Wert)
# TS_CONNECT_BACKUP_PASSWORD=
# Ändere den Port nur, wenn lokal bereits ein Postgres läuft
TS_CONNECT_BACKUP_PORT=5432

# Lemon Squeezy Lizenzprüfung
# Optional: API-Key für Lemon Squeezy (empfohlen)
TS_LICENSE_API_KEY=
# Mapping: Produkt- oder Varianten-IDs auf Plan (basic/plus/pro)
TS_LICENSE_VARIANT_PLAN_MAP=123456=basic,234567=plus,345678=pro
# Optional: Name/ID des Geräts für die Lizenzprüfung
TS_LICENSE_INSTANCE_NAME=ts-connect
TS_LICENSE_INSTANCE_ID=
TS_LICENSE_AUTO_ACTIVATE=true
TS_LICENSE_ACTIVATION_URL=https://api.lemonsqueezy.com/v1/licenses/activate

# Elastic Agent / Fleet
ELASTIC_AGENT_ENABLED=false
ELASTIC_FLEET_URL=https://fleet.example.com
ELASTIC_FLEET_ENROLLMENT_TOKEN=
ELASTIC_AGENT_ENVIRONMENT=prod
ELASTIC_AGENT_TAGS=site:unknown,tier:unknown
ELASTIC_AGENT_LOG_LEVEL=info

# Kafka Streams Transformer
TS_STREAMS_TARGET_PREFIX=ts.sds-test
# Optional Feintuning:
# TS_STREAMS_APPLICATION_ID=ts-streams-transform
# TS_STREAMS_INPUT_PATTERN=^[0-9]+\.SMDB\.(Schuetze|Treffer|Scheiben|Serien)$
# TS_STREAMS_NUM_THREADS=1
# TS_STREAMS_COMMIT_INTERVAL_MS=5000
